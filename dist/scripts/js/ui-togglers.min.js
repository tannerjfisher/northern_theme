"use strict";

/**
 * @file
 * UI toggler elements control
 */
(function ($, Drupal) {
  $(function () {
    // ===== Add overlay element
    $('.dialog-off-canvas-main-canvas').append('<div class="search-overlay"></div>');
  });
})(jQuery, Drupal);